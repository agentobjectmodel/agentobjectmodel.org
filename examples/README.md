# Examples

Examples are versioned. For the current release:

- **[v0.1.0](v0.1.0/)** — AOM surfaces, outputs, and demo agents. See [v0.1.0/README.md](v0.1.0/README.md) for layout, validation, and create-outputs instructions.

Run from repo root: `python aom.py validate all --examples-dir examples/v0.1.0` or `node aom.mjs validate all --examples-dir examples/v0.1.0`.
